
// 🧱 EJERCICIOS DE ESTRUCTURAS DE DATOS (PILAS, COLAS, ÁRBOLES, GRAFOS)

// 1️⃣ Implementa una pila (stack) desde cero con métodos push, pop, peek y isEmpty.

// 2️⃣ Implementa una cola (queue) desde cero con métodos enqueue, dequeue, front y isEmpty.

// 3️⃣ Dado un string con paréntesis, corchetes y llaves ((), [], {}), usa una pila para validar si están correctamente balanceados.
// Ejemplo: estaBalanceado("[()]{}") → true, estaBalanceado("[(])") → false

// 4️⃣ Crea un árbol binario de búsqueda (BST) con métodos insertar, buscar y recorrido en orden.

// 5️⃣ Dado un árbol binario, encuentra su profundidad máxima usando recursión.
// Ejemplo: maxDepth(root) → 3

// 6️⃣ Implementa un grafo no dirigido usando listas de adyacencia.
// Agrega métodos para agregar nodos, aristas, y recorrerlo con BFS y DFS.

// 7️⃣ Dado un grafo representado como objeto o mapa, encuentra si hay un camino entre dos nodos usando BFS.

// 8️⃣ Escribe una función que use una cola para simular el proceso de impresión de una impresora.
// Cada tarea tiene prioridad. La más alta se imprime primero.

// 9️⃣ Implementa una función para verificar si un árbol binario es un árbol binario de búsqueda válido (BST).

// 🔟 Simula una pila de deshacer/rehacer (undo/redo) en un editor de texto usando dos pilas.
// Operaciones: escribir, deshacer, rehacer.
