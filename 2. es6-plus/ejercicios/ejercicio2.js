// 1️⃣ Escribe una función que determine si dos palabras son anagramas (tienen las mismas letras en distinto orden).
// Ejemplo: "Roma" y "Amor" → true

const sonAnagramas = (str1, str2) => {
  // tu código aquí
};

// 2️⃣ Crea una función que reciba un texto y devuelva cuántas palabras únicas contiene.
// Ejemplo: "Hola hola mundo" → 2

const contarPalabrasUnicas = (texto) => {
  // tu código aquí
};

// 3️⃣ Implementa una función que determine si un número es un número feliz.
// Un número feliz termina en 1 después de reemplazar repetidamente el número con la suma de los cuadrados de sus dígitos.

const esNumeroFeliz = (numero) => {
  // tu código aquí
};

// 4️⃣ Dado un string, devuelve el primer carácter no repetido.
// Ejemplo: "aabccdeff" → "b"

const primerNoRepetido = (texto) => {
  // tu código aquí
};

// 5️⃣ Crea una función que reciba un número y devuelva true si es un número "armónico".
// Un número armónico es aquel en el que la suma de los divisores propios es mayor al número mismo.

const esArmónico = (numero) => {
  // tu código aquí
};

// 6️⃣ Dado un array de números, encuentra el subarray con la suma más alta (Kadane’s Algorithm level).
// Ejemplo: [−2,1,−3,4,−1,2,1,−5,4] → 6 (subarray: [4,−1,2,1])

const maxSubarraySuma = (arr) => {
  // tu código aquí
};

// 7️⃣ Implementa una función que compruebe si una cadena contiene todos los caracteres del alfabeto (pangrama).
// Ejemplo: "El veloz murciélago hindú comía feliz cardillo y kiwi" → true

const esPangrama = (frase) => {
  // tu código aquí
};

// 8️⃣ Escribe una función que determine si una matriz es un cuadrado mágico (la suma de filas, columnas y diagonales es igual).

const esCuadradoMagico = (matriz) => {
  // tu código aquí
};

// 9️⃣ Implementa una función que reciba un número entero y lo convierta a número romano.
// Ejemplo: 1994 → "MCMXCIV"

const aNumeroRomano = (numero) => {
  // tu código aquí
};

// 🔟 Crea una función que reciba un string con operaciones matemáticas simples y devuelva el resultado.
// Ejemplo: "3 + 5 * 2 - 4 / 2" → 11

const evaluarExpresion = (expresion) => {
  // tu código aquí
};


